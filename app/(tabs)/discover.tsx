import React from "react";

import { SmartMatchSwipeScreen } from "@/components/discovery/SmartMatchSwipeScreen";

/**
 * Smart Match — gợi ý từ API `GET /matching/suggestions/enhanced` (hồ sơ + matching engine).
 */
export default function DiscoverTabScreen() {
  return <SmartMatchSwipeScreen />;
}
